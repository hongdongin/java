package p07;




public class SyntexAndRule {

	public static void main(String[] args) {
		
		int 번호;
		int 韓;
		int num;
		int numStr;
		// int ln; 숫자 에러
		// int !n; 특수문자 에러
		// int @n;
		int _n;
		int $n; // 이 변수는 알고 있되 필드에서 왠만해서 사용하지 않는 것이 좋다
		
		// 패키지는 숫자로 만들 수가 없고 대문자로 만들 수 없다.
		// 클래스는 무조건 앞에 대문자로 쓴다. (한글, 한문도 가능하나 쓰지 않는 거시 좋다)
		
	  String str;
	  String[] str1; 
	}
}
